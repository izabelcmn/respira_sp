# Dependências da INTERFACE (isoladas do pacote do grupo).
streamlit>=1.40
pandas>=2.0
numpy>=1.26
plotly>=5.20
# Necessárias para carregar e rodar o LightGBM salvo:
lightgbm>=4.0
joblib>=1.3
scikit-learn>=1.3
# Opcional — assistente com LLM real:
# anthropic>=0.40
